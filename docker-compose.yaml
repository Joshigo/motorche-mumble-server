services:
  mumble:
    image: mumblevoip/mumble-server:latest
    container_name: mumble_server
    ports:
      - 64738:64738/tcp
      - 64738:64738/udp
    environment:
      - MUMBLE_SERVERNAME=Mumble Server
      - MUMBLE_MAXUSERS=50
      - MUMBLE_BANDWIDTH=72000
      - MUMBLE_CONFIG_SERVER_PASSWORD=${MUMBLE_PASSWORD}
      - MUMBLE_SUPERUSER_PASSWORD=${MUMBLE_SUPERUSER_PASSWORD}
      - MUMBLE_CONFIG_WELCOMETEXT='Welcome to Motorche Mumble Server'
    volumes:
      - ./mumble-data:/data
    restart: always